# 1. Постановка задачи

Вычислить определённый интеграл

```math
I=\int_{0}^{\pi}\frac{\sin(x^2)}{1-\cos x}\,dx
```

с использованием:

* составной формулы трапеций;
* составной формулы Симпсона;
* квадратурной формулы Гаусса–Лежандра с 11 узлами.

Требуемая точность вычислений:

```math
\varepsilon = 10^{-7}
```

Контроль точности выполнить по правилу Рунге.

---

# 2. Теоретические сведения

## 2.1 Составная формула трапеций

Пусть отрезок интегрирования `[a,b]` разбит на `N` равных частей.

Шаг сетки:

```math
h=\frac{b-a}{N}
```

Тогда составная формула трапеций имеет вид:

```math
I \approx
h\left(
\frac{f(a)+f(b)}{2}
+\sum_{k=1}^{N-1}f(x_k)
\right)
```

где

```math
x_k=a+kh
```

Погрешность метода имеет порядок

```math
O(h^2)
```

Следовательно, при уменьшении шага в два раза ошибка уменьшается примерно в четыре раза.

---

## 2.2 Вывод формулы Симпсона

На каждом элементарном отрезке функция заменяется интерполяционным многочленом второй степени.

Для участка

```math
[x_{k-1},x_k]
```

используются точки:

```math
x_{k-1},
\quad
x_{k-\frac12},
\quad
x_k
```

После интегрирования интерполяционного многочлена получаем формулу Симпсона:

```math
I \approx
\frac{h}{6}
\sum_{k=1}^{N}
\Bigl(
f(x_{k-1})
+
4f(x_{k-\frac12})
+
f(x_k)
\Bigr)
```

Порядок точности метода:

```math
O(h^4)
```

Поэтому метод Симпсона сходится значительно быстрее метода трапеций.

---

## 2.3 Правило Рунге

Для оценки погрешности используются два приближения:

```math
S_h
```

и

```math
S_{h/2}
```

Тогда оценка погрешности определяется формулой

```math
R_h=
\frac{|S_{h/2}-S_h|}
{2^p-1}
```

где:

* `p = 2` для метода трапеций;
* `p = 4` для метода Симпсона.

Если

```math
R_h < \varepsilon
```

то вычисления прекращаются.

Практический порядок точности вычисляется по формуле

```math
p_k=
\log_2
\left(
\frac{R_h}
{R_{h/2}}
\right)
```

---

## 2.4 Формула Гаусса–Лежандра

Квадратурная формула Гаусса–Лежандра имеет вид

```math
I\approx
\frac{b-a}{2}
\sum_{i=1}^{11}
w_i
f
\left(
\frac{b-a}{2}x_i
+
\frac{a+b}{2}
\right)
```

где:

* `x_i` — узлы квадратурной формулы;
* `w_i` — соответствующие веса.

Для данной лабораторной работы использовалась формула с 11 узлами.

---

# 3. Вычислительный эксперимент

Исследуемый интеграл:

```math
I=
\int_{0}^{\pi}
\frac{\sin(x^2)}
{1-\cos x}
dx
```

В программе реализованы:

1. Метод трапеций.
2. Метод Симпсона.
3. Формула Гаусса–Лежандра.
4. Автоматический контроль точности по правилу Рунге.

Точное значение интеграла было получено адаптивным методом Симпсона:

```math
I \approx 2.685612550602351
```

---

# 4. Результаты вычислений

Далее приводятся таблицы результатов, полученные программой.

(Вставить Таблицу 1)

(Вставить Таблицу 2)

---

# 5. Анализ результатов

Из таблицы метода трапеций видно, что практический порядок точности стремится к значению

```math
p \approx 2
```

что соответствует теории.

Для метода Симпсона практический порядок стремится к значению

```math
p \approx 4
```

что также подтверждает теоретические оценки.

Требуемая точность для метода Симпсона достигается при значительно меньшем числе разбиений, чем для метода трапеций.

Формула Гаусса–Лежандра с 11 узлами обеспечивает высокую точность вычислений уже без большого количества разбиений интервала.

---

# 6. Вывод

В ходе выполнения лабораторной работы были изучены методы численного интегрирования и реализованы программные алгоритмы вычисления определённого интеграла.

Были реализованы:

* составная формула трапеций;
* составная формула Симпсона;
* правило Рунге;
* квадратурная формула Гаусса–Лежандра с 11 узлами.

Полученные результаты подтвердили теоретические оценки порядков точности:

```math
p \approx 2
```

для метода трапеций и

```math
p \approx 4
```

для метода Симпсона.

Наиболее эффективным методом для рассматриваемого интеграла оказался метод Симпсона. Формула Гаусса–Лежандра показала наилучшую точность при минимальном объёме вычислений.


## 7. Графики
<img width="999" height="704" alt="{C65C196D-7A1C-4045-8A98-AD90978FE59A}" src="https://github.com/user-attachments/assets/0ea158d2-7653-43f5-8715-39312855212b" />
<img width="998" height="727" alt="{0B94FCDD-AD12-4C02-B27B-7B6619835D6A}" src="https://github.com/user-attachments/assets/8232dce4-4beb-40de-9b3d-2f18df44d76f" />
<img width="995" height="729" alt="{8801F0BB-FDEE-4AB4-B4F7-074BF13F8897}" src="https://github.com/user-attachments/assets/cde51e0a-4399-4624-8b65-3c41482154f9" />
