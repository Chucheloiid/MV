# 1. Постановка задачи

Рассматривается задача Коши

```math
u'=\frac{u^2\ln x-u}{x},
\quad x\in[1,2]
```

с начальным условием

```math
u(1)=0.5
```

Точное решение задачи имеет вид

```math
u(x)=\frac{1}{\ln x+x+1}
```

Требуется найти численное решение на равномерной сетке с шагами

```math
h=0.1
```

и

```math
h/2=0.05
```

с использованием неявного метода трапеций и метода Рунге–Кутты второго порядка, указанного в варианте задания.

Для решения нелинейного уравнения, возникающего на каждом шаге метода трапеций, используется метод Ньютона.

Требуется:

* получить численное решение задачи Коши;
* вычислить максимальную абсолютную ошибку относительно точного решения;
* выполнить оценку погрешности по правилу Рунге;
* построить графики точного и численного решений;
* сравнить полученные результаты.

---

# 2. Теоретические сведения

## 2.1 Неявный метод трапеций

Для дифференциального уравнения

```math
u'=f(x,u)
```

неявный метод трапеций записывается следующим образом:

```math
y_{i+1}
=
y_i+
\frac{h}{2}
\Bigl(
f(x_i,y_i)
+
f(x_{i+1},y_{i+1})
\Bigr)
```

В рассматриваемой задаче

```math
f(x,u)=\frac{u^2\ln x-u}{x}
```

Поэтому вычисление значения

```math
y_{i+1}
```

требует решения нелинейного уравнения.

Порядок точности метода:

```math
p=2
```

Следовательно, при уменьшении шага сетки в два раза погрешность должна уменьшаться примерно в четыре раза.

---

## 2.2 Метод Ньютона

Для нахождения значения

```math
y_{i+1}
```

вводится функция

```math
F(y)
=
y-y_i
-
\frac{h}{2}
\Bigl(
f(x_i,y_i)
+
f(x_{i+1},y)
\Bigr)
```

Требуется найти корень уравнения

```math
F(y)=0
```

Для этого используется метод Ньютона:

```math
y^{(k+1)}
=
y^{(k)}
-
\frac{F(y^{(k)})}
     {F'(y^{(k)})}
```

Производная правой части по переменной

```math
u
```

равна

```math
\frac{\partial f}{\partial u}
=
\frac{2u\ln x-1}{x}
```

Тогда производная функции Ньютона имеет вид

```math
F'(y)
=
1-
\frac{h}{2}
\frac{2y\ln x_{i+1}-1}
{x_{i+1}}
```

Итерации продолжаются до достижения заданной точности.

---

## 2.3 Метод Рунге–Кутты второго порядка

Коэффициенты метода Рунге–Кутты второго порядка:

```text
0   |
2/3 | 2/3
----+---------
    | 1/4  3/4
```

Вычисления выполняются по формулам

```math
k_1=f(x_i,y_i)
```

```math
k_2=
f\left(
x_i+\frac23h,
y_i+\frac23hk_1
\right)
```

```math
y_{i+1}
=
y_i+
h
\left(
\frac14k_1+
\frac34k_2
\right)
```

Метод имеет второй порядок точности.

---

## 2.4 Оценка погрешности по правилу Рунге

Для метода порядка

```math
p
```

оценка погрешности вычисляется по формуле

```math
R=
\frac{\|y^h-y^{h/2}\|_\infty}
     {2^p-1}
```

Для метода трапеций и метода Рунге–Кутты

```math
p=2
```

поэтому

```math
R=
\frac{\|y^h-y^{h/2}\|_\infty}
     {3}
```

Максимальное значение вычисляется по всем узлам сетки.

---

# 3. Вычислительный эксперимент

Для решения задачи была реализована программа на языке Java.

В программе реализованы:

1. Правая часть дифференциального уравнения.
2. Неявный метод трапеций.
3. Итерационный метод Ньютона.
4. Метод Рунге–Кутты второго порядка.
5. Вычисление максимальной ошибки.
6. Оценка погрешности по правилу Рунге.
7. Построение графиков точного и численного решений.

Расчёты выполнялись для двух шагов сетки:

```math
h=0.1
```

и

```math
h=0.05
```

---

# 4. Результаты вычислений

## Неявный метод трапеций

### Максимальная ошибка при шаге h = 0.1

```text
6.825804184041329E-4
```

### Максимальная ошибка при шаге h = 0.05

```text
1.7078860826508224E-4
```

### Отношение ошибок

```math
\frac
{6.825804184041329\cdot10^{-4}}
{1.7078860826508224\cdot10^{-4}}
\approx 3.997
```

---

## Метод Рунге–Кутты второго порядка

### Максимальная ошибка при шаге h = 0.1

```text
3.252768854539734E-4
```

### Максимальная ошибка при шаге h = 0.05

```text
7.977348483800828E-5
```

### Отношение ошибок

```math
\frac
{3.252768854539734\cdot10^{-4}}
{7.977348483800828\cdot10^{-5}}
\approx 4.076
```

---

## График решений

На рисунке представлены:

* точное решение;
* решение, полученное неявным методом трапеций;
* решение, полученное методом Рунге–Кутты второго порядка.

На всём интервале интегрирования графики практически совпадают.

<img width="1020" height="776" alt="{CA6BB9DF-D67D-490A-9CBD-62BF59B7EE76}" src="https://github.com/user-attachments/assets/69757026-72f2-4342-a7bb-700625e98d44" />


---

# 5. Анализ результатов

Полученные результаты показывают, что оба численных метода обеспечивают высокую точность решения задачи Коши.

Для неявного метода трапеций уменьшение шага сетки в два раза привело к уменьшению ошибки приблизительно в

```math
3.997
```

раза.

Для метода Рунге–Кутты второго порядка уменьшение шага привело к уменьшению ошибки приблизительно в

```math
4.076
```

раза.

Теоретически для методов второго порядка точности при уменьшении шага в два раза ошибка должна уменьшаться примерно в

```math
2^2=4
```

раза.

Полученные значения близки к теоретическому результату, что подтверждает правильность реализации алгоритмов и соответствие их второму порядку точности.

Построенный график показывает практически полное совпадение численных решений с точным решением на всём интервале интегрирования.

---

# 6. Вывод

В ходе выполнения лабораторной работы была решена задача Коши для нелинейного дифференциального уравнения первого порядка.

Для получения численного решения были реализованы неявный метод трапеций и метод Рунге–Кутты второго порядка. Для решения нелинейных уравнений, возникающих в неявном методе трапеций, использовался метод Ньютона.

Проведено сравнение численных решений с точным решением задачи. Полученные ошибки оказались малы, а уменьшение шага сетки в два раза привело к уменьшению ошибки примерно в четыре раза, что соответствует теоретическому второму порядку точности методов.

Построенные графики подтвердили высокую точность численных методов. Полученные результаты свидетельствуют о корректности программной реализации и эффективности рассмотренных методов для решения задачи Коши.
